# route-migration-map

既存URLは削除しない。`/profile` は表示名を「問い」に変更し、旧「関心マップ」は画面内の「根拠を見る」として扱う。

- `/labs` -> 維持。研究領域・学会・ジャーナル検索を追加。
- `/discover` -> 維持。混合デッキへ拡張。
- `/saved` -> 維持。問いの芽と問いプロジェクト導線を追加。
- `/profile` -> 維持。ナビ表記を「問い」に変更。
- `/labs/:id` -> 維持。関連領域・学会・ジャーナル候補を追加。
